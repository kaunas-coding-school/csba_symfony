{% extends 'base.html.twig' %}

{% block title %}Meniu{% endblock %}

{% block body %}
    <h1>Meniu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ meniu.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ meniu.name }}</td>
            </tr>
            <tr>
                <th>Path_name</th>
                <td>{{ meniu.pathName }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ meniu.path }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('meniu_index') }}">back to list</a>

    <a href="{{ path('meniu_edit', {'id': meniu.id}) }}">edit</a>

    {{ include('meniu/_delete_form.html.twig') }}
{% endblock %}
